server {
    listen 80;

    # Set the root directory for serving files
    root /usr/share/nginx/html;
    
    # Try file, then try directory, then fallback to /index.html
    # This is crucial for single-page applications (SPAs)
    location / {
        try_files $uri $uri/ /index.html;
    }
}